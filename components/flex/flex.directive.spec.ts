import { FlexDirective } from './flex.directive';

describe('FlexDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexDirective();
    expect(directive).toBeTruthy();
  });
});
