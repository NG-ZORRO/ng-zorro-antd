---
order: 15
title:
  zh-CN: 前缀和后缀
  en-US: Prefix and Suffix
---

## zh-CN

分别使用 `nzPrefix` 和 `nzSuffixIcon` 自定义前缀和后缀图标。

## en-US

Custom `prefix` and `suffixIcon` by using respectively `nzPrefix` and `nzSuffixIcon`.
