---
order: 14
title:
  zh-CN: 高精度小数
  en-US: High precision decimal
---

## zh-CN

通过 `nzStringMode` 开启高精度小数支持，`ngModelChange` 将返回 string 类型。

## en-US

Use `nzStringMode` to support high precision decimals. `ngModelChange` will emit a `string` value instead of a `number`.
